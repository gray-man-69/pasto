// Base path the app is served under. Empty for localhost / root hosting; set to
// "/pasto" for GitHub Pages via NEXT_PUBLIC_BASE_PATH at build time (inlined).
export const BASE_PATH = process.env.NEXT_PUBLIC_BASE_PATH ?? "";
